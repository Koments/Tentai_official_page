import {LeftSideProps} from "./components/feedback-left/types";
import {RightSideProps} from "./components/feedback-right/types";

export type  FeedbackProps = {
    leftSide: LeftSideProps
    rightSide: RightSideProps
};